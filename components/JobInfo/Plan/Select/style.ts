import styled from 'styled-components'

const FormWrapper = styled.div`
  display: flex;
  flex-direction: column;
  margin: 10px 20px;
`
const FormLabel = styled.label`
  font-weight: 500;
  font-size: 16px;
  line-height: 24px;
  color: #222529;
  margin-bottom: 12px;
`

export { FormWrapper, FormLabel }
