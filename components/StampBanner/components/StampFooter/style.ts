import styled from 'styled-components'

const ModalFooterWrapper = styled.div`
  background-color: rgba(0, 117, 129, 0.05);
  background-image: linear-gradient(
    135deg,
    transparent,
    rgba(255, 100, 191, 0.1)
  );
`

export { ModalFooterWrapper }
